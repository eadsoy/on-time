
<div class="container">
  <div class="row">
    <div id="map" 
        class="col-6"
        style="width: 50%; height: 500px;"
        data-markers=""
        data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
    </div>
    <div class="col-6">
      <div id="directions" class="directions"></div>

      <button type="button" id='btn-submit' class='btn btn-danger d-inline'>Create Route</button>

      <button type="button" id='btn-try' class='btn btn-danger d-inline'>TRY</button>
      <button type="button" id='btn-try-again' class='btn btn-danger d-inline'>TRY AGAIN</button>

      <%= form_with url: "/tracks", method: :post do |form| %>
        <%= form.label :query, "Search for:" %>
        <%= form.submit "Find songs" %>
      <% end %>


      <%= link_to 'Sign in with Spotify', user_spotify_omniauth_authorize_path, class: 'btn btn-dark' %>
    </div>
  </div>
</div>

<div class="container">
  <div class="spotify row">
  
  </div>
</div>




<%#= simple_form_for @route do |f| %>
    <%#= f.input :start, as: :string, input_html: { id: 'search'} %>
    <%#= f.input :end, as: :string, input_html: { id: 'search-end'} %>
    <%#= f.submit "Send", class: 'btn btn-dark' %>
<%# end %>